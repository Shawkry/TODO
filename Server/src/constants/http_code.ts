export enum Http_CODE {
  SUCCESS = 200,
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  BAD_REQUEST = 400,
  CREATED = 201,
}
export enum CODE {
  ERROR,
  SUCCESS,
}
